if productos.length > 0
    div(class="table-responsive")
        table(class="table table-dark")
            tr(style="color: yellow; text-align: center;") 
                th Producto
                th Precio
                th Imagen

            each producto in productos 
                tr
                    td(style="text-align: center;")= producto.producto
                    td(style="text-align: center;")= producto.precio
                    td
                        div(style="text-align: center")
                            img(style="width: 80px; height:80px;" src= producto.URL alt="")
       
else
    h3(class="alert alert-danger") No se encontraron datos
